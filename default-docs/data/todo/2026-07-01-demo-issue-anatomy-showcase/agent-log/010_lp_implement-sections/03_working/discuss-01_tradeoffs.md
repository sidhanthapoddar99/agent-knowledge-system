---
agent: claude-opus-4-8
date: 2026-07-01
---
# Discussion — one reader vs three

Saved dialogue weighing a single generalized reader against keeping three separate ones.
Landed on one `readFreeformDocs(dir, dataPath, issueId, subName)`: the shapes are
identical and the `subName` parameter is enough to vary labels and URLs. Kept as raw
provenance behind milestone #1 — the *what we looked at*, not the *what we concluded*.
