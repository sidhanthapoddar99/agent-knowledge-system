---
title: "Skills updates for diagram embeds"
status: open
---

Once the three embed subtasks in this group settle the syntax, the plugin
skills must teach it — embeds are the everyday path (diagrams that don't need
to be first-class live in `assets/` and are referenced).

## Tasks

- [ ] **`documentation-guide` skill** — add the embed syntax for all three
      formats (inline fence + reference form) to the writing reference
      (`references/writing.md`), including when to reach for which format.
- [ ] **`doc-issues` skill** — tracker-flavoured guidance in
      `references/10_writing/10_writing.md`: diagrams in issues live under the
      issue's `assets/` (which is never sidebar-visible) and are embedded by
      reference.
- [ ] **Mirror to plugin cache** — repo-local plugin source and installed
      cache must stay identical; apply every skill edit to both.
