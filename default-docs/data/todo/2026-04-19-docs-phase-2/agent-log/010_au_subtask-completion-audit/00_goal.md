---
title: "Goal — audit subtasks 01–08 against the shipped docs"
---

All eight subtasks on this issue sat at `open` even though months of docs work
(lifecycle waves, theme-token docs, skill-catalogue rewrites) had landed since
they were written. The user asked for an evidence-based check: which subtasks
are actually complete in the docs tree, regardless of their recorded status?

**Done looks like:** every subtask (01–08) has a verdict — done / partial /
not done — each backed by concrete file paths under
`default-docs/data/user-guide/` or `dev-docs/`, with genuinely complete ones
moved to `review` (agent ceiling; `done` is the human's call) and the rest left
`open` with the gap named. Read-only against the docs — no doc edits in this
run.
