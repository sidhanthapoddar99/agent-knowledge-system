---
title: "Summary — the skill now carries the variation-set pattern + design-memory doctrine"
---

## What shipped (2026-07-08)

The full remedy accepted on subtask
[100](../../subtasks/100_variation-capability-audit.md), in two milestones:

- **[M1](101_skill-authoring.md)** — the "Variation-set / options-explorer
  artifacts" section in `references/design-systems.md` (four defining
  qualities as requirements, structure-by-count, treatment override, worked
  skeleton, Home-A misreading corrected at source) + the four SKILL.md hooks
  (trigger description, triage row, §0 decision-tooling bullet, §1 canonical-
  case cross-link, §3 **Operable** gate item). Emphasis placed per the
  live-test delta: shared-fixture realism and the shippable-polish bar are
  called out as what even good runs miss.
- **[M2](102_doctrine-parity-wiring.md)** — sidecar-as-design-memory doctrine
  (read before / update after; anti-drift duty stated where agents actually
  look), self-mode token naming preference (contract names, own values, free
  additions — pattern snippet rewritten to demonstrate it), `variation-set`
  sidecar type + keys, README "Built for agents, observable by humans"
  section, repo ↔ cache byte parity, tracker records.

## Verification

`diff -rq` repo skill ↔ installed cache clean; `agent-ks check issues`
clean. Subtask 100 handed off at `review` (human closes).

## The through-line

The audit ([060](../060_au_variation-capability/01_summary.md)) proved the
pattern was unwritten and the verify gate interaction-blind; the live
cold-Sonnet test (brainstorm
[10_variation-comparison](../../brainstorm/10_variation-comparison/01_delta-analysis.md))
proved a good run still misses fixture realism and polish; this activity wrote
exactly those findings into the skill, so the next cold agent inherits them
instead of rediscovering them.
