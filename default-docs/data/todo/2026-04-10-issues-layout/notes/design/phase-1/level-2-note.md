# Notes — level 2 (depth 2, the cap)

This is for testing the 2-level subfolder tree (subtask 20).

This note lives at `notes/design/phase-1/level-2-note.md` — two folders below the `notes/` root. This is the deepest level the loader accepts; anything deeper than this is logged as a warning and ignored. It should appear nested under `design > phase-1` in the sidebar.
