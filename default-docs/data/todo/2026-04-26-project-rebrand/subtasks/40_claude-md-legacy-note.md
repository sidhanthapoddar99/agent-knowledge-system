---
title: "CLAUDE.md legacy note — ~3 lines on the old naming"
status: done
---

A deliberate, bounded exception to the history-free rule (which covers skills
and published docs, not CLAUDE.md): a short note — about three lines — telling
future sessions that the project was rebranded from **documentation-template**
to **agent-knowledge-system**, that tracker history and old commits use the
legacy names (`docs-guide` CLI, `documentation-guide` / `doc-issues` /
`artifact-authoring` skills), and that only current names may be used in new
work. Prevents an agent reading an old issue from "helpfully" reintroducing a
retired name.

## Tasks

- [x] Write the note (≤3 lines) in CLAUDE.md near the Skills + tooling section.
- [ ] Drop it once the rename has fully settled (old names gone from all open
      issues and the archive flow is live) — the note itself says when it can
      be deleted.
