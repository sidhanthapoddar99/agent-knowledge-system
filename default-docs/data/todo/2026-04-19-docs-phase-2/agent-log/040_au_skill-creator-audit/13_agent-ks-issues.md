---
title: agent-ks-issues — Opus review
---

# agent-ks-issues

**Verdict:** needs fixes — the structure is right and the writing is disciplined, but the skill's own precedence rule points at a bundled user guide that is now stale in three verified places, four claims are false against the engine and the CLI, one instruction the agent needs every session sits in an orphaned file, and SKILL.md is 30% over the word cap on content its references already carry.

**Measured:** SKILL.md 779 words body / 869 words with frontmatter (cap 600, over by 179) · references: 01_anatomy 134 lines, 02_lifecycle 59, 03_writing 51, 04_issue-comments-glossary 98, 05_brainstorm-notes-memory 110, 06_subtasks 150 (cap is *under* 150), 07_plans 133, 08_agent-logs 5, 09_operations 124, 10_examples 108.

Clean on: relative links (`agent-ks check skill-links` passes, 60 files), no site-absolute links, no shouty `MUST`/`NEVER`/`ALWAYS`, history-free (no "legacy", "used to", "renamed"), templates match the five-section claim byte for byte.

## Findings

| # | Severity | Where | What | Why it matters | Fix |
|---|---|---|---|---|---|
| 0 | blocker | `SKILL.md:10` | "when the framework folder is present, its bundled user guide at `<framework>/default-docs/data/user-guide/19_issues/` wins over this skill. When the two disagree, follow the guide, update this skill." That guide is stale in three places I verified: it documents an agent log as `01_summary.md` + `02_working/` + `03_debrief/` (`03_folder-structure.md:83,105,128`; `05_sub-docs/05_agent-log.md:79`) while `agent-ks issue new-agent-log` writes `00_index.md` from `templates/log-index-<kind>.md`; it lists **five** agent-log kinds with no `re` (`05_agent-log.md:146-154`) while `DEFAULT_AGENT_LOG_KINDS` in `issues.ts:337-343` has six including `re`; and it never mentions the five-section body template (`01 To Do` … `05 Notes & Analysis`) that `templates/subtask.md` writes and `check issues --template` enforces — its own subtask example (`05_sub-docs/03_subtasks.md:63-76`) would fail that check | This rule turns a stale document into an instruction to regress the skill. An agent that hits any of the three and obeys `SKILL.md:10` will write `01_summary.md`, refuse a `re` log, and strip the numbered sections out of a subtask — then "update this skill" to match | Narrow the precedence rule: the **engine and the CLI** are the source of truth for anything they implement (`issue-status.ts`, `issues.ts`, `agent-ks help`, `templates/`); the user guide wins only on convention it alone states. Then file a separate issue to re-sync `user-guide/19_issues/` — it is out of this skill's scope but it is the root cause |
| 1 | blocker | `references/03_writing.md:20` | "`draft: true` hides the file from the production build" is false for every tracker file — `issues.ts` reads `draft` only from an issue's `settings.json` (line 1565) and the tracker root (line 1537); `readSubtaskFile` and `readFreeformDocs` never read it | The agent marks a subtask or note draft, believes it is hidden, and it ships live — and `check issues` reports `draft` as unknown-key drift, because `SUBTASK_FM_KEYS` and `NOTE_FM_KEYS` in `check.mjs:83-86` do not list it | Delete the `draft: true` clause. Say instead: to hide a whole issue, set `"draft": true` in its `settings.json`; there is no per-file draft in a tracker |
| 2 | major | `references/03_writing.md:20` | "`color:` tints the sidebar label" is stated as a blanket rule over a table that includes subtask and plan `overview.md`. The loader reads `fm.color` only in `readFreeformDocs` (notes, brainstorm, agent-memory — `issues.ts:1093`) and `readAgentLogs` (`:1165`); `readSubtaskFile` (`:1429`) reads `title` and `status` only | On a subtask `color:` does nothing and earns a drift warning, so the agent writes a field, sees no tint, and hunts a theme bug | Move `color:` out of the blanket sentence into the rows that own it — note, brainstorm, agent-memory, agent-log file. Say plainly it is ignored on a subtask and a stage |
| 3 | major | `references/09_operations.md:9` | "an unrecognised flag is ignored in silence" is false. `agent-ks-dev issue list --bogus-flag xyz` prints `unknown flag --bogus-flag`, lists every valid flag, and exits 2 | It teaches a wrong failure model. An agent whose filter returns nothing will suspect a typo the CLI already ruled out, and will not suspect the real cause (the default non-Closed scope, two lines below) | Replace the row with the true behaviour: an unknown flag is a hard error with the valid list and exit 2. Then the row below — the Closed-scope tip — becomes the only silent-narrowing case, which is the point |
| 4 | major | `references/01_anatomy.md:56` and `:74`, `references/10_examples.md:72` and `:82` | Child agent logs are documented as `1NN_` "from 100", and the worked example uses `100_wf_shared-fixture/`. The one home for the fact (`agent-ks-issue-logs/SKILL.md:55,77`) and the scaffolder (`new-agent-log.mjs:119`, `const floor = nested ? 120 : 10`) both start child logs at `120` and reserve `100`/`110` for a results or debrief folder | The agent hand-writes a `100_` child log into the slot the convention reserves, then the scaffolder disagrees with it on the next run | Change both anatomy lines to "a child log `1NN_` from 120; 100 and 110 stay free" and renumber the example to `120_wf_shared-fixture/`. Better: drop the number from anatomy and link `agent-ks-issue-logs`, which owns it |
| 5 | major | `SKILL.md:66` | The Never row reads "`au` and `rf` need no ask". `agent-ks-issue-logs/SKILL.md:50` says `au`, `rf` **and** `re` open without asking | The same rule in two files has drifted. An agent following SKILL.md will stop and ask before opening a research log that the owning skill says needs no ask | Add `re`, or better delete the parenthetical and let the row say only "ask before `lp`, `wf` or `it`", with the exception list linked to the owning skill |
| 6 | major | `references/08_agent-logs.md` (whole file) | The file is not in SKILL.md's Triage table — the agent-log row points past it, straight at the sibling skill. Its only inbound link is `10_examples.md:28`. Yet `08:5` carries the one instruction needed at the start of every continued session: "Before you continue an issue, read the open log's `00_index.md` and `agent-memory/memory.md` first" | The highest-value line in the skill is in the file the routing table never names, so it is read by accident or not at all | Promote that one sentence into SKILL.md as the pickup step (see finding 7) and delete `08_agent-logs.md`. Its other three sentences duplicate the sibling skill's opening |
| 7 | major | `SKILL.md` (absent) | There is no pickup flow. SKILL.md gives duties, boundaries, statuses, prohibitions and a triage table, and never says what to open first. The sequence exists but is split three ways: `04:5` ("read `issue.md` first"), `08:5` (open log + memory), `10:25` (`issue show` → `issue.md` → `issue agent-logs`) | "Continue work on issue X" is the most common trigger this skill has, and it is the one task SKILL.md does not answer. The agent guesses a Triage row or reads all ten references | Add a four-line "Pick up an issue" block at the top of SKILL.md: `agent-ks issue show <id>` → `issue.md` → `agent-memory/memory.md` → the open log's `00_index.md`. That is ~35 words and it replaces three scattered statements |
| 8 | major | `SKILL.md:19-39` | "Sections and duties" (9 rows) and "The four boundaries" (4 rows) are ~200 words restating what `04`, `05`, `06` and `07` each state again in their own Holds / Does-not-hold tables. `05:3` even says "Duties: the section table in SKILL.md" and then prints its own | This is the whole 179-word overage, and it is the one-home rule broken by the file that states the one-home rule | Cut "The four boundaries" entirely — every row of it is already the first paragraph of the reference that owns it. Shrink "Sections and duties" to Section + Owns, two columns, and let each reference carry "Never holds" |
| 9 | major | `SKILL.md:3` (description) | "Outside the tracker (docs, blog, site config, themes, images) use agent-ks-docs." `agent-ks-docs`'s own description disclaims four of those five: blog → `agent-ks-blog`, site config / themes / layouts → `agent-ks-config` | The routing line is always in context and sends the agent to a skill that refuses the job, costing one full skill load before the correct hop | Rewrite the tail as: "Outside the tracker: docs pages → agent-ks-docs, a blog post → agent-ks-blog, site config / themes → agent-ks-config" |
| 10 | major | `SKILL.md:3` (description) | The description claims "agent logs" and "record agent progress" as triggers, and lists "agent logs" among what it covers. `agent-ks-issue-logs` owns that domain and says so in its own description | A prompt like "open an audit log and write the handover" loads the 779-word umbrella skill first and reaches the right one on the second hop | Keep "agent logs" as a *routing* mention, not a trigger claim: "…notes and plans. Agent logs have their own skill, agent-ks-issue-logs." Drop "record agent progress" from the trigger list |
| 10a | major | `references/05_brainstorm-notes-memory.md:13` | "Brainstorm has no volume rule. Frontmatter is `title` only." False — `brainstorm` uses the `freeform` reader (`issue-sections.ts:75`), and `readFreeformDocs` reads `fm.color` (`issues.ts:1093`). `check.mjs:775` applies `NOTE_FM_KEYS`, which lists `color`. The bundled guide agrees (`05_sub-docs/06_brainstorm.md:49-51`) | The skill tells the agent a valid, rendered field is forbidden. `04_issue-comments-glossary.md:90` then tells it to write a colour legend "when the issue uses `color:`" — the two references disagree inside one skill | "Frontmatter is `title` and optional `color`, the same as a note." |
| 11 | minor | `references/06_subtasks.md` | 150 lines. The house rule is *under* 150 | Marginal, but the cap exists so a reference stays one screen-and-a-bit | Cut the `## Rapid mechanical changes` block (`:148-150`) — it is one decision the agent makes once, and it duplicates the "when a run earns a log" table in `agent-ks-issue-logs` |
| 12 | minor | `references/01_anatomy.md:63` | "The only root files are `issue.md`, `settings.json` and `glossary.md`. The validator warns on any other root file." `check.mjs:673` filters `e.name.endsWith('.md')`, so a root `notes.txt` or `data.json` passes silently | Over-promises the validator. Small, but it is stated as a checked rule and it is a convention | "The validator warns on a stray `.md` at the root. Other stray files are not checked" |
| 13 | minor | `references/05_brainstorm-notes-memory.md:110` | "`agent-ks check issues` warns when `memory.md` is missing." `check.mjs:812` guards on `fs.existsSync(memDir)` first, so the warning fires only when `agent-memory/` exists without an index | An agent could read this as "every issue is warned into having memory", which the validator does not do | "…warns when `agent-memory/` exists without a `memory.md` index" |
| 14 | minor | `references/01_anatomy.md:81-94` | The per-issue settings table marks `priority`, `component`, `labels`, `author`, `assignees` "Required: yes", then line 94 says a missing `labels` or `assignees` reads as `[]`. Only `title` and `status` are hard errors (`check.mjs:576-577`); an empty `component` is a warning (`:591`); `priority`, `author` and `assignees` are never checked for presence | The table contradicts its own footnote, and an agent auditing a tracker will report non-defects as schema violations | Split the column into "Hard error if missing" (`title`, `status`) and "Convention" (the rest), or add a Level column: error / warning / convention |
| 15 | minor | `SKILL.md:52` | "A run uses five: `open`, `in-progress`, `input-needed`, `done`, `dropped`." Correct (`issue-status.ts:59` `RUN_STATUSES`), but it is needed only when writing an agent log — the sibling skill's job — and it is stated again at `02_lifecycle.md:32` | A sometimes-fact in the always-loaded file, and a third copy of a value the engine already calls out as over-duplicated (`issue-status.ts:56` names five copies in skill prose) | Delete the line from SKILL.md. `02_lifecycle.md` and `agent-ks-issue-logs` both carry it |
| 16 | minor | `SKILL.md:19-31` | The duties table names "plan overview" and "plan stage" but never the `plans/` folder, and `subtasks/`, `notes/`, `brainstorm/` appear with their trailing slash while plans do not | An agent that reads only SKILL.md does not learn the folder is called `plans/` | Add the slash: `plans/` overview, `plans/` stage |
| 17 | minor | `references/06_subtasks.md:136` | The create step gives `agent-ks issue new-subtask <id> --name <slug> [--group <g>] [--title <t>]` and omits `--overview <text>`, which writes the lead paragraph the very next step tells the agent to fill by hand | Two passes over one file where one would do, on the most-run scaffolder in the skill | Add `[--overview <t>]` to the flag list |
| 18 | minor | `references/09_operations.md:35-45` | "Delegate bulk reads" is 11 lines of generic subagent strategy (patterns A/B/C, a breakeven number) inside a tracker operations reference | Only pattern B is tracker-shaped. The rest is orchestration policy that belongs to whoever orchestrates, and it is loaded every time an agent wants the `move` command | Cut to two lines: push a >10-file read onto a subagent, stage the exact `agent-ks` commands, ask for a bounded answer. Drop the A/B/C taxonomy |
| 19 | minor | `references/01_anatomy.md:73`, `references/06_subtasks.md:47` | "One level is the convention" for `subtasks/` grouping. The bundled guide says up to 3 levels is the recommended convention, cap 5 (`05_sub-docs/03_subtasks.md:33,58-59`). Both agree the hard cap is 5, which is the only part in code (`order-prefix.ts:41`) | Two documents state two different conventions for the same thing, and `SKILL.md:10` says the guide wins — so the rule the agent follows depends on which file it opened | Pick one and state it once. If one level is the intent, say why (a group is an area, not a phase — `06:26` already argues it) and get the guide changed to match |
| 20 | minor | `references/01_anatomy.md:87` | "One per issue … The validator warns on more. The one exception is `issue-dump`." No `issue-dump` carve-out exists: `check.mjs:595` warns whenever `components.length > 1`, with no component-value test, and the string `issue-dump` appears nowhere in `check.mjs` or `issues.ts`. The sentence is also ambiguous — it can read as an exception to the warning or to "name a layer" | The agent expects no warning on a dump issue and gets one, or tags a dump issue with several components on the strength of a rule that is not there | Either delete the clause, or say plainly what is meant: "`issue-dump` names a holding pen rather than a layer; it still takes exactly one component" |
| 21 | minor | `references/04_issue-comments-glossary.md:30` | "Keep the file between 50 and 300 lines." The bundled guide says 50–200 (`05_sub-docs/04_notes.md:129`) | A soft number stated twice with two values. Low impact, but it is the kind of drift the one-home rule exists to stop | State it once, in whichever file owns `issue.md` sizing, and link from the other |

Verified true, in case a later round doubts them: the eight statuses and four categories (`issue-status.ts:20-40`); `RUN_STATUSES` (`:59`); `fields.status` and `statusColors` are hard errors (`check.mjs:531`, `issue-status.ts:231`); the depth cap is 5 (`order-prefix.ts:41`); the prefix grammar is 2–5 digits (`:24-25`); artifacts render in `brainstorm/` and `notes/` only (`issue-sections.ts:75,82` `allowArtifacts: true`, everything else `false`); diagrams render in those plus `agent-memory/` and `agent-log/` (`issues.ts:1047,1168`); the stage redirect exists (`static-paths.ts:181-189`); `agentLogKinds` takes both the object and the `"hf": "hotfix"` string form (`issues.ts:914-922`); the `subtasks:` and `agent-logs:` validator errors (`check.mjs:474,503,505`); the two-open-plans warning (`:518`); the index-leaf derived-status warning (`:752`); the `## Agent log`, `## Questions` and template-section warnings (`:203,214,218,243,183`); `--status all` works; `--tracker` is on every `issue` verb; the bun requirement (`bin/agent-ks:20`); the worktree `.env` note (`_env.mjs:122`); the hidden-Closed stderr tip (`list.mjs:272-281`); every template file the skill names exists and matches the five-section shape. The six agent-log kinds the skill claims (`01_anatomy.md:27`) are right and the bundled guide is the stale one — `DEFAULT_AGENT_LOG_KINDS` has `re`.

Method note for findings 0, 19, 20, 21: all 11 skill files were cross-read against all 22 files of `default-docs/data/user-guide/19_issues/`. Every conflict listed above was then re-verified by me against the engine source or the live CLI, so the ruling on which side is stale rests on code, not on the guide's authority. Two further guide conflicts were checked and dismissed as guide-only staleness with no skill defect: the guide's `01_summary.md` agent-log shape (folded into finding 0) and its free-form subtask body examples, which `check issues --template` would itself flag.

## Trigger test

| Prompt | Should fire | Fires |
|---|---|---|
| "the loader-refactor subtask on docs-phase-2 is done my side — write up the result and flip it to review" | yes | yes — "subtask", "review", "status" are all in the trigger list |
| "add an issue for the version-gate precheck, high priority, component versioning" | yes | yes — "issue", "priority", "component" |
| "i keep rediscovering the same three sidebar-tree gotchas every session, where do i park that so the next run has it" | yes (this is `agent-memory/`) | unsure — "agent memory" is in the *coverage* clause but no trigger word matches how a user actually says it. The trigger list is all tracker nouns; nothing catches "remember", "next session", "don't rediscover" |
| "open an audit log for the skill review and write the handover" | no — `agent-ks-issue-logs` owns it | yes, wrongly — the description claims "agent logs" and "record agent progress" as its own (finding 10) |
| "write the release note for 0.3.8 under releases/" | no | no — correctly outside every trigger word |

## Proposed description

> Use for any work in an agent-knowledge-system issue tracker (`data/todo/`, or any folder-per-issue tracker): issues, subtasks, plans and stages, comments, brainstorms, notes, agent memory, glossaries, the tracker vocabulary, the review queue and the issue dump. Also use for an audit, a refactor, a loop, an autonomous run or a discussion on a tracked issue, and whenever you must remember something for the next session on an issue. Trigger on issue, ticket, subtask, tracker, backlog, priority, component, label, status, plan, stage, or any file under a tracker folder — and on "where do I record this", "pick this back up", "what did we decide". Agent logs have their own skill: agent-ks-issue-logs. Outside the tracker: docs pages → agent-ks-docs, a blog post → agent-ks-blog, site config or themes → agent-ks-config.

Three changes, each fixing a finding above: the agent-log claim becomes a hand-off (10), the outside-the-tracker route names the right three skills (9), and the trigger list gains the phrases a user says when they mean `agent-memory/` (trigger test row 3).

## The dry run

Prompt: *"I'm picking the docs-phase-2 issue back up — where did we get to, and what's next?"*

1. SKILL.md loads: duties, boundaries, statuses, a Never table, a Triage table. **Lost immediately** — nothing says what to open first.
2. Triage has no row for "continue an issue". Guessed between "issue body, comments, glossary" and "examples".
3. Read `04_issue-comments-glossary.md`. Line 5 pays off — "Read it first when you pick up an issue" — but that belongs in SKILL.md, and `04` says nothing about memory or an open log.
4. Read `10_examples.md` for the flow. Line 25 gives `agent-ks issue show <id>` → `issue.md` → `agent-ks issue agent-logs <id>`. Still no memory.
5. The complete answer — "read the open log's `00_index.md` and `agent-memory/memory.md` first" — is `08_agent-logs.md:5`, which the Triage table never names. Found it by listing `references/`, not by following the skill.
6. Did not need: `01_anatomy.md` (134 lines, the tree and the settings schema) and `03_writing.md` (51 lines) — both loaded on the guess that "pick up" meant structure.
7. Missing outright: a start-of-session sequence in one place. It exists in three fragments across `04`, `08` and `10`, and no file names all three steps.
8. Once oriented, everything else worked. `06_subtasks.md` answered "what's next" cleanly, `09_operations.md` gave the right search command on the first read, and `agent-ks-dev issue list --status all` behaved exactly as `09:17` describes.

## Cut and add

**Cut**

- `SKILL.md:33-39` — "The four boundaries", 4 rows. Every one is the opening paragraph of the reference that owns it (`06:13`, `07:5-12`, `05:45-50`, `05:9-12`).
- `SKILL.md:19-31` — shrink "Sections and duties" to two columns. The "Never holds" column is restated in full by `04`, `05`, `06` and `07`.
- `SKILL.md:52` — the five run statuses. Sometimes-needed, and `02_lifecycle.md:32` owns it.
- `references/08_agent-logs.md` — delete the file after moving its one unique sentence into SKILL.md (finding 6). Sentences 1–3 duplicate `agent-ks-issue-logs/SKILL.md:9-15`.
- `references/09_operations.md:35-45` — the A/B/C delegation taxonomy, down to two lines.
- `references/06_subtasks.md:148-150` — "Rapid mechanical changes"; it duplicates the log-or-no-log decision the sibling skill owns, and cutting it puts the file under the 150-line cap.

Together these take SKILL.md from 779 body words to roughly 560, under the cap, with the pickup block (finding 7) added.

**Add**

- A "Pick up an issue" block at the top of SKILL.md, four steps, ~35 words. This is the single highest-value addition in the report.
- In `03_writing.md`, one line stating what a tracker file's frontmatter may **not** carry, since `check issues` reports drift: a subtask takes `title` and `status` only; `color` belongs to note, brainstorm, agent-memory and agent-log files; there is no per-file `draft`.
- In `06_subtasks.md:136`, the `--overview` flag.
- In `01_anatomy.md`, a Level column on the settings table (error / warning / convention) so an auditing agent reports the right severity.
- In `SKILL.md:10`, name what the engine and the CLI own versus what the user guide owns (finding 0). One sentence, and it is the difference between the skill self-correcting and the skill self-regressing.
- In `05_brainstorm-notes-memory.md:13`, `color` on the brainstorm frontmatter line, so the file stops contradicting `04_issue-comments-glossary.md:90`.
