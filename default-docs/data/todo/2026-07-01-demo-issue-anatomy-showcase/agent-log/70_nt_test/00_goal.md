---
agent: claude-opus-4-8
status: in-progress
date: 2026-07-01
---
# Goal — undefined-code probe
`nt` is **not** one of the standard codes and is **not** declared in this issue's
`agentLogKinds`. Fallback expectation: no kind pill; the folder keeps its numeric prefix
badge and `nt` stays visible in the label ("nt test"). 2-digit prefix (`70`) coexisting
with the 3-digit ones (`010`–`060`).
