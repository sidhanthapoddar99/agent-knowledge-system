---
author: claude
date: 2026-08-07
---

# Part of this issue's dead-code sweep already ran, elsewhere

[The Astro 7 issue's dead-code sweep](../../2026-08-07-astro-7-and-load-time-refactor/subtasks/050_cleanup/010_dead-code-sweep.md)
ran today and overlaps this issue's own sweep. Posting so it is not done twice.

**What is already deleted** — verified against all 188 source files for every
import form before removal, not taken on the audit's word:

| | |
|---|---|
| 7 unreferenced editor files | **707 lines** |
| `@astrojs/mdx` + its `astro.config.mjs` entry | zero `.mdx` files exist |

The 7 files were `editor/core/wysiwyg-decorations.ts` (438),
`editor/layout/shell.ts` (99), `editor/layout/preview-panel.ts` (51),
`editor/core/codemirror-languages.ts` (43), `editor/util/lazy-import.ts` (42),
`editor/util/prefix-utils.ts` (27) and `editor/layout/shell-styles.ts` (7).

**What was deliberately NOT deleted, and should not be swept here either:**

- **The presence system** (267 lines). The audit called it dead and the evidence
  holds — no client, `users` never populated. But it is the server half of
  multi-user editing, which is a wanted feature, and it is still wired into
  `integration.ts`, `yjs-sync.ts` and the cache-inspector. It is Sid's call to
  finish or delete, not a cleanup decision.
- **`cache-manager`'s ~120 unused lines.** Owned by
  [its own subtask](../../2026-08-07-astro-7-and-load-time-refactor/subtasks/030_correctness/020_cache-manager-dependency-tracking.md),
  and the partial-rebuild work now argues for *implementing* them rather than
  deleting — they are the dependency tracking an incremental build would need.

**Also worth knowing before any sweep here:** a `tsc --noUnusedLocals` pass turns
up 13 unused locals and parameters across 12 files. They were left alone —
touching 12 files for no behaviour change is churn. The list is in the subtask
above so nobody re-derives it.
