---
title: Decide the rules — when to create issues + the issue dump
status: done
---

Decide and write down the operating rules for the new structure, so the sections
don't blur back together in practice.

- [x] When to create a new issue vs add to an existing one
- [x] When something is Brainstorm vs Notes vs Subtask vs Agent-log
- [x] When deliberation graduates from `brainstorm/` into `notes/`
- [x] Rules for Comments staying lean (evolution log, not a forum)
- [x] **A dedicated "issues dump"** — a lightweight place to jot down random ideas /
      half-formed issues without the ceremony of a full issue folder. Decide its
      shape (a single backlog issue? a top-level `inbox/`? captured via a command —
      see subtask 04) and the rule for graduating a dump entry into a real issue.

**Decided 2026-07-02 — full rules written to `notes/02_operating-rules.md`.** Key
calls: litmus test (component + first subtask in one breath); thinking/doing ×
in-motion/settled router; graduation triggered by downstream citation; two-paragraph
comment tripwire; dump = an `issue-dump` component category holding a few dump issues
(entries as subtasks, graduated entries **deleted**, not ticked). All convention —
nothing code-enforced. Deciding is done — closed 2026-07-02. Propagating the rules
into the skill, docs, and guide.ts is split out to subtask 07 (this issue) alongside
lifecycle issue subtasks 04–06.

Related: `2026-07-02-issue-lifecycle-and-creation-rules` now owns the *threshold*
question (when a thought earns a full issue vs a subtask vs a brainstorm entry) plus
the expanded lifecycle states. The first two bullets above coordinate with its
subtask 01 — link, don't duplicate.
