---
title: "Summary — skill strengthened, cache mirrored, 12 offenders catalogued"
---

Single-milestone burst (2026-07-08) executing subtask 09 against
`notes/02_agent-log-activity-structure.md`. The milestone-frontmatter
requirement (with the `done`-is-wrong-vocabulary trap) and the per-kind
mapping-unit table now live in SKILL.md's executing-work section and
`24_agent-logs.md`; `guide.ts` got the one-line legend. Repo source and
installed cache verified identical; framework build green.

The tracker sweep found 12 pre-existing milestones with the same gaps (details
in [101](101_skill-and-guide-updates.md)) — left untouched, backfill optional.

A second pass after user review
([102](102_reference-tree-rhythm-notes-bridge.md)) raised adherence to
"default rhythm + hard invariants": the per-kind reference tree now lives in
the skill itself, the recipe became the scaffold → stub `in-progress` → update
→ wrap lifecycle, and dense run outputs are pointed at `notes/` (+ the
artifact-authoring skill) with milestones linking rather than inlining.
Remaining: verify the structure holds on the next real run (subtask 09's last
open item).
