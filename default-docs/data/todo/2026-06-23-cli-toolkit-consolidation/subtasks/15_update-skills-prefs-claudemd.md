---
title: "Update skills, preferences, and CLAUDE.md"
state: closed
---

Second-to-last execution step. Once the command surface is final, sync the agent-facing instruction layer so the new commands, `help` discovery, and contract are documented where agents read them.

## Tasks

- [ ] `CLAUDE.md` (repo root) — update the CLI wrapper tables (counts, new commands: `docs help`, `docs-check-issues`, `find`, `git`, docs/blog list/show/search), note the `help`/`--json` discovery convention
- [ ] `documentation-guide` skill (`SKILL.md` + reference tables, esp. `references/layouts/issues/41_searching.md`) — new flags (`--path`/`--meta`/`--count`), new commands, the uniform contract
- [ ] `doc-agent` skill — if any agent-log/loop command surface changed
- [ ] Preferences / project conventions — record the "use `docs help` first to discover commands" guidance for agents
- [ ] Keep the skill catalogue page in sync (`user-guide/.../claude-skills.md`)
