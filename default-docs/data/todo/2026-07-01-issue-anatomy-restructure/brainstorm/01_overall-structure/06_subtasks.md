# Subtasks

**Scope:** the plan — a checklist of what's to be done, each with a state
(`open` / `review` / `closed` / `cancelled`).

Related rule: for fast change-bursts, a subtask can serve as a **running checklist** — see
"Fast bursts" in `07_agent-log` for when that beats an agent-log activity.

## To discuss

- Nested `NN_<group>/` subtask folders exist (with their own `settings.json`) — anything
  to settle about when to group vs keep flat?
- Best-practice: AI-handoff-bound issues declare ≥1 subtask — should the validator hint
  this, or stay skill-guidance only?

## Decided

- Largely **unchanged from today**: prefixed files, states, the Comprehensive panel, the
  per-subtask index in the right rail.
- Created **after** notes settle the approach; updated as loops close them out. This is
  the *what*; agent-log records the *how*.
