# Second-level nesting probe

This file lives two levels below `brainstorm/` (`01_options/02_nested/`), rendering a
nested `<details>` subgroup. Nesting now goes up to 5 levels — see `04_nesting-demo/` for
the full-depth walk.
