---
title: "Goal — execute subtask 10: agent-ks CLI + agent-ks-* skill rename"
---

Execute the first slice of the locked rebrand (agent-knowledge-system,
2026-07-08): rename the plugin's three skills to `agent-ks-docs` /
`agent-ks-issues` / `agent-ks-artifacts` and the CLI dispatcher `docs-guide` →
`agent-ks`, then sweep every live surface (plugin internals, CLAUDE.md,
READMEs, user-guide, dev-docs, source comments/strings, settings, installed
cache) so no live reference speaks the old vocabulary. Tracker history stays
untouched. Work runs on branch `rebrand/agent-ks`.

Naming evolved live during scoping: `agent-doc` / `agent-doc-*` was applied
first, then sidhantha tightened it to `agent-ks` / `agent-ks-*` to match the
locked project name (the whole point of the rebrand is escaping the "doc"
framing) — both passes are in this activity.
