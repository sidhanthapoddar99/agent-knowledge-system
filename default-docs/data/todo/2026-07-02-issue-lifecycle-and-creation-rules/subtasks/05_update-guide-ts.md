---
title: Update the bundled guide.ts issue-anatomy panel
state: open
---

`astro-doc-code/src/layouts/issues/default/guide.ts` is the plugin-independent legend
rendered on every issue's Guide panel — it must describe the same lifecycle as the
docs and skill.

- [ ] New state list + categories in the lifecycle section
- [ ] Remove any assignees-as-in-progress phrasing
- [ ] Keep it a thin map — depth stays in the user-guide / skill
